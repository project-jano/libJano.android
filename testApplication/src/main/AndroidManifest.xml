<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.jano.mobile.tests.libs.application">

    <application
        android:name=".TestApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestAppTheme">
        <activity
            android:name="io.jano.mobile.tests.libs.application.views.securedpayload.ViewSecuredPayloadActivity"
            android:exported="true"
            android:label="@string/title_activity_view_secured_payload"
            android:theme="@style/Theme.TestAppTheme.NoActionBar" />
        <activity
            android:name="io.jano.mobile.tests.libs.application.views.main.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".services.AppMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>